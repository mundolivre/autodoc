#include <string>

/**
 *  @brief Pega a versão do sistema operacional
 * @param OSVersion variável de saída que irá conter a versão do sistema operacional
 */
void GetOperationalSystemVersion(std::string &OSVersion);

void FunctionWithoutDoc();
